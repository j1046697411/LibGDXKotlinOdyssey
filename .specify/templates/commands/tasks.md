# speckit.tasks

When generating tasks, ensure categories include:

- Testing tasks (new tests + edge cases)
- Coverage tasks (maintain/raise ≥ 80%)
- Quality tasks (ktlint/detekt)
- Performance tasks (benchmarks if needed)

Reference: `.specify/memory/constitution.md`.

